import { WishCategoryDirective } from './wish-category.directive';

describe('WishCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new WishCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
