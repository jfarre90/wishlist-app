import { WishDirective } from './wish.directive';

describe('WishDirective', () => {
  it('should create an instance', () => {
    const directive = new WishDirective();
    expect(directive).toBeTruthy();
  });
});
